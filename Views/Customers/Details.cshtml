@model FortuneTellerMVC2.Models.Customer

@{
    ViewBag.Title = "Details";
}

<h2>Details</h2>

<div>
    <h4>Customer</h4>
    <hr />
    @Model.FirstName @Model.LastName will retire in @ViewBag.NumberOfYears years with @ViewBag.AmountOfMoney in the bank,
    a vacattion home in @ViewBag.Location 
    and a @ViewBag.Transportation

</div>
<p>
    @Html.ActionLink("Edit", "Edit", new { id = Model.CustomerID }) |
    @Html.ActionLink("Back to List", "Index")
</p>
